#pragma once
#include "glm/glm.hpp"

namespace glm::vector3
{
	const vec3 zero (0, 0, 0);
	const vec3 one (1, 1, 1);
	const vec3 forward (1, 0, 0);
	const vec3 right (0, 1, 0);
	const vec3 up (0, 0, 1);
	const vec3 down (0, 0, -1);
}